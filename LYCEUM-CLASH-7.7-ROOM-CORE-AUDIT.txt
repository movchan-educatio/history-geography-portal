LYCEUM CLASH 7.7 — ROOM CORE ROLLBACK

Цього разу зроблено не ще один workaround, а повернення мережевого ядра до 7.0 — версії, в якій кімнати у вас реально створювалися.

КОНКРЕТНО ЗНАЙДЕНО
У 7.6 код викликав bellClashApplyRoomState(...), але визначення цієї функції після попередніх патчів уже було втрачено.
Node --check цього не показує, тому що це runtime-помилка, а не синтаксична.

Також room core був надмірно ускладнений REST/HTTP/ETag/timeout шарами.

ПОВЕРНУТО З РОБОЧОЇ 7.0
• bellClashCreateRoom
• bellClashJoinRoom
• bellClashWatchRoom
• bellClashWritePlayer
• bellClashSetRoomMap
• bellClashSetBotSettings
• bellClashStartRoom
• bellClashApplyRoomState
• bellClashForceRoomSync
• bellClashStopJoinSync
• bellClashStartJoinSync
• bellClashWatch
• Create/Join UI
• HOST Start UI

CREATE ROOM ЗНОВУ:
1. Генерує 5-символьний код.
2. get(roomRef) перевіряє код.
3. set(roomRef, room) створює кімнату.
4. Відкривається lobby.
5. Підключається realtime watcher.

ЄДИНА МАЛА ЗМІНА
onDisconnect запускається без await і не може затримати відкриття lobby.

ПРИБРАНО
• Firebase REST create
• HTTP fallback
• ETag room transactions
• HTTP room polling
• token timeout layer
• transport-status UI

ЗБЕРЕЖЕНО
• графіку нових версій;
• великі карти;
• до 5 героїв;
• менших героїв на арені;
• покращені скіни/зброю;
• SMART BOT AI та local fallback;
• Focus UI;
• 75-секундні матчі.

ПЕРЕВІРКА
• portal-score.js — node --check OK.
• Усі inline JavaScript — node --check OK.
• Усі критичні room/sync функції існують рівно один раз.
• bellClashApplyRoomState реально визначена.
• Create Room знову використовує робочі get() + set().
• REST/HTTP room layer повністю видалено.
